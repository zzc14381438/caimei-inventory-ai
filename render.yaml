services:
  - type: web
    name: caimei-inventory-ai
    runtime: python
    buildCommand: pip install -r requirements.txt && python migrate_to_sqlite.py
    startCommand: python caimei_api.py
    envVars:
      - key: DEEPSEEK_API_KEY
        sync: false
    plan: free
    region: singapore
